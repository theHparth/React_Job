import React from "react";
import Wrapper from "../asserts/wrappers/BigSidebar";

const BigSidebar = () => {
  return (
    <Wrapper>
      <h4>Big side bar</h4>
    </Wrapper>
  );
};

export default BigSidebar;
